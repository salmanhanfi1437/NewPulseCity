pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    id("com.facebook.react.settings") // React Native Gradle Plugin
}

rootProject.name = "ZuvyStore"

apply(from = file("../node_modules/react-native-unimodules/gradle.groovy"))

include(":app")
